import 'package:flutter/material.dart';

class CropeCard extends StatelessWidget {
  const CropeCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Card(child: Container(color: Colors.red,));
  }
}
